using UnityEngine;

public class CheckpointManager : MonoBehaviour
{
    //[SerializeField] private Gameobject[] checkpoints; // Array of checkpoints for the car to follow
    Checkpoint currentCheckpoint; // Reference to the current checkpoint
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
